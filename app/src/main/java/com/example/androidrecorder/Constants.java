package com.example.androidrecorder;

import android.os.Environment;

public class Constants {
    public static final String VIDEO_PATH = Environment.getExternalStorageDirectory().getAbsolutePath() + "/vivo.mp4";
    public static final String PATH = Environment.getExternalStorageDirectory().getAbsolutePath() + "/VideoDemo";
}
